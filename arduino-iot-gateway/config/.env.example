# ==============================================================================
# BrainSAIT Arduino IoT Gateway - Environment Configuration
# ==============================================================================
# Copy this file to .env and configure with your values.
# NEVER commit .env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# Serial Port Configuration
# ------------------------------------------------------------------------------
# macOS: /dev/cu.usbmodem1201 (check with: ls /dev/cu.*)
# Linux: /dev/ttyUSB0 or /dev/ttyACM0 (check with: ls /dev/tty*)
# Windows: COM3 (check with: mode in PowerShell)
SERIAL_PORT=/dev/cu.usbmodem1201
BAUD_RATE=115200

# ------------------------------------------------------------------------------
# BrainSAIT Cloud API Configuration
# ------------------------------------------------------------------------------
# Domains:
#   - Landing Page: https://brainsait.cloud
#   - App Dashboard: https://sbs.brainsait.cloud
#   - IoT Events API: https://sbs.brainsait.cloud/api/v1/iot/events
#
# For local development, use mock server:
#   API_URL=http://localhost:8000/ingest
#
# For production:
API_URL=https://sbs.brainsait.cloud/api/v1/iot/events

# ------------------------------------------------------------------------------
# Security Token
# ------------------------------------------------------------------------------
# Generate token from: https://sbs.brainsait.cloud/admin/tokens
# Rotate tokens every 90 days for security compliance.
# Leave empty to skip cloud sync (useful for local testing).
API_TOKEN=

# ------------------------------------------------------------------------------
# Device Identification
# ------------------------------------------------------------------------------
# NODE_ID: Unique identifier for this edge device
# FACILITY_CODE: Healthcare facility code (for multi-site deployments)
NODE_ID=BS-EDGE-001
FACILITY_CODE=HQ

# ------------------------------------------------------------------------------
# Logging
# ------------------------------------------------------------------------------
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
