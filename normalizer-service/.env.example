# SBS Normalizer Service – environment example
# Copy this file to .env and fill in the secrets before running locally.
# Never commit the populated .env; treat its values as secrets managed outside git.

# --- Database configuration ------------------------------------------------
DB_HOST=postgres
DB_NAME=sbs_integration
DB_USER=sbs_api_user
DB_PASSWORD=replace-with-your-db-secret
DB_PORT=5432

# --- CORS/Origins ----------------------------------------------------------
ALLOWED_ORIGINS=https://app.staging.sbs.com,http://localhost:3000
# If you only need a single origin, set CORS_ORIGIN instead (this takes precedence).
CORS_ORIGIN=

# --- AI provider configuration --------------------------------------------
AI_PROVIDER=deepseek               # gemini | deepseek | custom
GEMINI_API_KEY=
DEEPSEEK_API_KEY=sk-your-deepseek-key
AI_URL=                           # Optional custom LLM endpoint

# --- AI fallback service (services/normalizer/ai_fallback.py) --------------
AI_FALLBACK_ENDPOINT=https://ai.internal.sbs/api/map
AI_FALLBACK_API_KEY=replace-with-fallback-api-key

# --- Operational defaults --------------------------------------------------
CERT_BASE_PATH=/certs
CERT_PASSWORD=

# Security notes ------------------------------------------------------------
# 1. Pull secrets from Vault, AWS Secrets Manager, Azure Key Vault, or equivalent.
# 2. Rotate any key that leaves your secrets boundary and purge git history if necessary.
# 3. Automate env injection in CI/CD rather than baking secrets into image builds.
